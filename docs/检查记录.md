# 本次交付检查记录

日期：2026-09-18

## 已通过

- 使用 package-lock.json 完成一次干净 npm ci 安装（Windows、Node 24.15）。
- npm test：7/7 通过。
- npm run build：生产构建通过。
- Prettier：源码、测试、配置与文档格式检查通过。
- 生产构建 dist 通过本地静态预览服务器打开，页面入口正常。
- 浏览器验证：月历、周视图、事项切换，公司筛选，艺人搜索，中文/英文/泰文切换，活动详情、两张图片轮播和安全外链。
- 浏览器验证：收藏状态切换、事项在相邻日期持续显示、中国官方标签描边颜色。
- 手机宽度 390px：检查月历、周视图、事项，以及英文、泰文页面。修复英文月份标题溢出后，文档宽度不超过视口。
- 日期测试：跨月区间含两端、闰年、6 行月份、跨年。
- 数据测试：错误日期/倒置日期拒绝、字符串 false 不误判为官方、不安全链接过滤。
- Apps Script 模拟测试：官方身份来自来源配置、额外私密列不进入公开响应、重复 ID/缺失 ID 拒绝。

## 尚未验证

- 用户提供的真实 Apps Script 地址：多种连接尝试超时；未拿到 JSON 响应，无法确认现有字段和是否需要额外参数。
- 未在用户 Google 账号中部署或执行配套 Code.gs；测试只验证其输出逻辑，不等于 Google 权限和跨域联调已完成。
- 未发布线上网站，未改动远端仓库或表格。
- 未测试真实移动设备、Safari 或所有浏览器；本次检查使用 Chromium 浏览器。
- 录入管理网站及其鉴权、编辑、删除、批量导入不属于本次展示端交付。

如果现有接口格式不符合 docs/数据接口与接入.md，需要拿到实际响应或脚本源码后继续适配。
